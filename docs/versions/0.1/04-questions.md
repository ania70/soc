# سوالاتِ دور ۳ — Questions round 3 (Data Model)

> به هر سوال در ستونِ «پاسخِ تو» جواب بده. اگر خالی بگذاری، پیش‌فرضِ پیشنهادی قبول شده است.

| # | سوال | چرا مهم است | گزینه‌ها | پیش‌فرضِ پیشنهادی | پاسخِ تو |
|---|------|-------------|---------|-------------------|----------|
| ۱ | آیا «نود» و «پاد» چیزهای جداگانه‌اند یا فقط از رویِ رویدادها استخراج می‌شوند؟ | اگر جداگانه ذخیره شوند، می‌توان وضعیتِ پروب روی هر نود را نشان داد؛ اگر از رویدادها استخراج شوند، ساده‌تر است ولی وضعیتِ پروب از دست می‌رود | چیزِ جداگانه · از رویدادها استخراج · فقط نود جداگانه | **فقط نود جداگانه** — پادها از رویدادها استخراج می‌شوند (نیازی به ذخیرهٔ جداگانه نیست) ولی نود جداگانه ذخیره می‌شود تا وضعیتِ پروب نشان داده شود | |
| ۲ | آیا باید بدانیم کدام کاربر کِش واردِ داشبورد شده؟ (session/audit log) | برای محصولِ امنیتی ممکن است مهم باشد، ولی در ۰.۱ فقط یک admin هست | بله · خیر | **خیر** — در ۰.۱ فقط یک admin هست؛ audit log در ۰.۲ | |
| ۳ | آیا رویدادها نیاز به شناسهٔ یکتا دارند یا ترتیبِ زمانی کافی است؟ | شناسهٔ یکتا کمک می‌کند در پیمایشِ صفحه‌بندی رویدادها تکرار نشوند یا جا نیفتند | بله · خیر | **بله** — هر رویداد یک شناسهٔ یکتای خودکار می‌گیرد | |
| ۴ | وقتی یک پاد خراب می‌شود و دوباره ساخته می‌شود با همان نام، آیا رویدادهای پادِ قدیمی و جدید باید از هم جدا باشند؟ | پادها در کوبرنتیز پویا هستند — نام یکسان ولی شناسهٔ متفاوت | بر اساسِ نام · بر اساسِ شناسهٔ کانتینر · فرقی نمی‌کند | **بر اساسِ شناسهٔ کانتینر** — نامِ پاد می‌تواند تکراری باشد ولی کانتینرِ آیدی یکتاست | |
| ۵ | آیا آرگومان‌های هر نوعِ syscall به‌صورتِ فیلدهای جداگانه ذخیره شوند یا به‌صورتِ یک رشتهٔ JSON؟ | فیلدهای جداگانه جستجو را راحت‌تر می‌کند ولی پیچیدگیِ ذخیره‌سازی بیشتر است | فیلدهای جداگانه · JSON · ترکیبی | **JSON** — برای ۰.۱ ساده‌تر است؛ فیلدهای جداگانه در ۰.۲ وقتی جستجو اضافه می‌شود | |
| ۶ | آیا وضعیتِ پروب روی هر نود (فعال/غیرفعال) در نسخهٔ ۰.۱ لازم است؟ | اگر یک نود از کار بیفتد، کاربر باید ببیند | بله · خیر | **بله** — یک ستونِ وضعیت در صفحهٔ نودها | |
| ۷ | وقتی رویدادها پاک می‌شوند (بر اساسِ `EVENT_RETENTION_HOURS`)، آیا نودها و پادها هم پاک شوند؟ | نودها باید باقی بمانند تا وضعیتِ پروب نشان داده شود؛ پادها می‌توانند پاک شوند | نودها بمانند · همه بمانند · همه پاک شوند | **نودها بمانند، پادها از رویدادها استخراج می‌شوند** — پس پاکسازیِ رویدادها به‌طورِ خودکار پادهای بدونِ رویداد را هم پاک می‌کند | |
