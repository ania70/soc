# سوالاتِ دور ۱ — Questions round 1

> به هر سوال در ستونِ «پاسخِ تو» جواب بده. اگر خالی بگذاری، پیش‌فرضِ پیشنهادی قبول شده است.

| # | سوال | چرا مهم است | گزینه‌ها | پیش‌فرضِ پیشنهادی | پاسخِ تو |
|---|------|-------------|---------|-------------------|----------|
| ۱ | دمو روی چه محیطی اجرا می‌شود؟ یک کلاسترِ واقعیِ K8s، یا kind/minikube روی یک ماشین؟ | تعیین می‌کند چه چیزی باید نصب شود و پیچیدگیِ دمو چقدر است | کلاسترِ واقعی · kind · minikube · فقط یک ماشینِ لینوکس بدون K8s | **kind** روی یک ماشینِ لینوکس — سبک، قابلِ نصب در چند دقیقه، و برای دمو کافی | |
| ۲ | چه syscall هایی در نسخهٔ ۰.۱ پوشش داده شوند؟ | هر syscall که اضافه شود پیچیدگیِ پروب را بیشتر می‌کند | فقط `execve` · `execve` + `openat` · `execve` + `openat` + `connect` · همه | **`execve` + `openat` + `connect`** — سه syscall که بیشترین ارزشِ امنیتی را دارند و دمو را گویا می‌کنند | |
| ۳ | زبانِ پیاده‌سازیِ پروبِ eBPF چه باشد؟ | تعیین می‌کند چه ابزارهایی لازم است و چه‌قدر توسعه سریع است | Go + cilium/ebpf · C + libbpf · Python + bcc · bpftrace | **Go + cilium/ebpf** — مدرن، مستنداتِ خوب، و با داشبورد (FastAPI) از طریقِ gRPC/REST ارتباط می‌گیرد | |
| ۴ | آیا دمو باید روی یک ماشینِ واقعیِ لینوکس اجرا شود یا می‌تواند داخلِ یک کانتینر باشد؟ | eBPF به کرنلِ لینوکس نیاز دارد و داخلِ کانتینر محدود است | ماشینِ واقعی · کانتینر با `--privileged` · VM | **ماشینِ واقعیِ لینوکس** — eBPF در کانتینر محدود است؛ برای دمو یک ماشینِ لینوکس (فیزیکی یا VM) ساده‌ترین است | |
| ۵ | چه کسی کاربرِ هدفِ این دمو است؟ تیمِ امنیت، مدیرِ زیرساخت، یا مدیرِ فنی؟ | طراحیِ داشبورد و زبانِ رابطِ کاربری را تعیین می‌کند | تیمِ امنیت · مدیرِ زیرساخت · مدیرِ فنی · همه | **تیمِ امنیت** — کسانی که باید رفتارِ مشکوک را ببینند و واکنش نشان دهند | |
| ۶ | آیا محصول در نسخهٔ ۰.۱ فقط رویدادها را ثبت و نمایش می‌دهد، یا باید فیلتر/جستجو هم داشته باشد؟ | فیلتر پیچیدگیِ داشبورد را بیشتر می‌کند ولی برای دمو ارزشِ زیادی دارد | فقط ثبت و نمایش · ثبت + فیلترِ ساده · ثبت + فیلتر + جستجو | **ثبت + فیلترِ ساده** (بر اساسِ نوعِ syscall، نامِ پاد، namespace) — بدونِ جستجوی کامل | |
| ۷ | آیا رویدادها باید به‌صورتِ زنده (streaming) در داشبورد نمایش داده شوند یا فقط به‌صورتِ تاریخی؟ | streaming پیچیدگیِ فنی دارد ولی برای دمو تأثیرِ بصریِ زیادی دارد | فقط تاریخی · فقط زنده · هم زنده هم تاریخی | **هم زنده هم تاریخی** — زنده برای تأثیرِ دمو، تاریخی برای بررسی | |
| ۸ | آیا برای نسخهٔ ۰.۱ نیاز به احرازِ هویت در داشبورد هست؟ | احرازِ هویت زمانِ توسعه می‌گیرد ولی محصولِ امنیتی بدونِ آن غیرحرفه‌ای است | بدونِ احراز · نامِ کاربری و رمز · SSO | **نامِ کاربری و رمز** — ساده، سریع، و برای دمو کافی | |
| ۹ | آیا محصول باید روی کلاسترِ خودِ شما (git.kubit.dev) اجرا شود یا یک محیطِ جداگانه؟ | eBPF به کرنلِ لینوکس نیاز دارد و محیطِ اجرا مهم است | روی کلاسترِ موجود · محیطِ جداگانه | **محیطِ جداگانه** — eBPF روی کرنلِ لینوکس اجرا می‌شود و بهتر است جداگانه باشد تا با زیرساختِ موجود تداخل نداشته باشد | |
| ۱۰ | از بینِ سه مرحلهٔ محصول (مانیتورینگ، شناساییِ رفتار، تشخیص و واکنش)، کدام سه مورد برای دموی ۰.۱ حیاتی هستند؟ | این تعیین می‌کند چه چیزی در ۰.۱ ساخته می‌شود و چه چیزی به ۰.۲+ می‌رود | (پاسخِ آزاد) | **فقط مانیتورینگ** در ۰.۱؛ شناساییِ رفتار در ۰.۲؛ تشخیص و واکنش در ۰.۳ | |
| ۱۱ | آیا رقیبِ اصلیِ شما Falco است یا رقیبِ دیگری در ذهن دارید؟ | تعیین می‌کند مزیتِ رقابتی را چطور فرمول‌بندی کنیم | Falco · Tetragon · Tracee · رقیبِ دیگر | **Falco** — معروف‌ترین و پراستفاده‌ترین در این حوزه | |
| ۱۲ | آیا شما به کرنلِ لینوکس نسخهٔ ۴.۱۸ یا بالاتر دسترسی دارید؟ | eBPF در کرنل‌های قدیمی‌تر محدود است | بله · خیر · مطمئن نیستم | **بله** — اکثرِ توزیع‌های مدرن (Ubuntu 20.04+, CentOS 8+) این را دارند | |
